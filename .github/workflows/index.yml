name: Rebuild file index

# Someone dropping files into files/ through the GitHub web UI is the normal
# way this repo gets content, so the index has to rebuild itself rather than
# depend on anyone running a script.
on:
  push:
    branches: [main]
    paths:
      - 'files/**'
      - 'tools/build_index.py'
  workflow_dispatch:

permissions:
  contents: write

# A burst of uploads through the web UI is one push per file. Without this they
# race each other and all but one fail to push.
concurrency:
  group: rebuild-index
  cancel-in-progress: false

jobs:
  index:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Build index.json
        run: python tools/build_index.py

      - name: Commit if the tree actually changed
        run: |
          if git diff --quiet -- index.json; then
            echo "index.json unchanged"
            exit 0
          fi
          git config user.name  "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index.json
          git commit -m "Rebuild file index"
          git push
