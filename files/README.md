# files

Everything here is browsable inside the app. Put files in any folder structure
you like — the app mirrors it exactly: a folder shows as a folder, a file shows
as a file.

The folders already here are a starting point, not a rule. Rename them, delete
them, add others.
